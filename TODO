take into consideration early progressions (all on by default)

below map have resources (which maybe just opens the FF1R wiki)
click map to open full image
have a hints notepad
would be cool to save/reset state
